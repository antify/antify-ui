export enum Type {
    email = 'email',
    password = 'password',
    tel = 'tel',
    text = 'text',
    url = 'url',
}
export enum TextInputColorType {
    base = 'base',
    danger = 'danger',
    info = 'info',
    success = 'success',
    warning = 'warning',
}

